# Cambiar el dominio antes de deployar (env var DOMAIN)
{$DOMAIN:metasuite.local} {
	# Basic auth para el dashboard, excluye /webhook/* y /health.
	# Generar hash: docker run --rm caddy:2-alpine caddy hash-password
	# Descomentar y cargar env BASIC_AUTH_USER + BASIC_AUTH_HASH cuando esté en internet.
	# @dashboard not path /webhook/* /health
	# basic_auth @dashboard {
	# 	{$BASIC_AUTH_USER} {$BASIC_AUTH_HASH}
	# }

	reverse_proxy api:3000
}
